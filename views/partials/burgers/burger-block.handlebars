<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-neweat="{{eat}}">
		{{#if eat}}Eat the burger{{else}}""{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{id}}">
		Remove Burger
	</button> --}}
</li>
