<h1>Burgers!</h1>

<h2>Burgers that are not eatBurger!</h2>

<ul>
  {{#each burgers}}
    {{#unless eatBurger}}
      {{> burgers/burger-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are eatBurger!</h2>

<ul>
  {{#each burgers}}
    {{#if eatBurger}}
      {{> burgers/burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="eatBurger" value="1" checked> Sleepy!<br>
    <input type="radio" name="eatBurger" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>
